- name: file variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml   #This imports variables from an external YAML file named course.yaml.
  tasks:
  - name: print variables
    ansible.builtin.debug:
      msg: "Course is: {{ COURSE }}, Trainer is: {{ TRAINER }}" # variables will be printed from external course.yaml file.