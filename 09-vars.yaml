- name: variables from args
  hosts: local
  connection: local
  tasks:
  - name: print variables from args
    ansible.builtin.debug:
      msg: "Hi {{ NAME }}, Good Morning" # The variable is expected from the command line arguments. If there is no declation at Inventory.