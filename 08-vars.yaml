- name: inventory variables
  hosts: local
  connection: local
  tasks:
  - name: print variables
    ansible.builtin.debug:
      msg: "Course is: {{ COURSE }}" #The variable is expected to come from the inventory file. Inventory variables are usually defined inside the inventory file.